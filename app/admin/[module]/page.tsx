import Link from "next/link";

export function generateStaticParams(){return ['customers','bookings','fleet','kyc','enquiries','support'].map(module=>({module}));}
export default async function ModulePage({params}:{params:Promise<{module:string}>}){const {module}=await params;const title=module.charAt(0).toUpperCase()+module.slice(1);return <main className="min-h-screen bg-slate-100 text-slate-900"><header className="bg-slate-950 text-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5"><Link href="/admin" className="text-xl font-black">ALLINO CRM</Link><Link href="/admin" className="rounded-xl border border-white/20 px-4 py-2 text-sm font-bold">Dashboard</Link></div></header><div className="mx-auto max-w-7xl px-5 py-8"><p className="text-sm font-bold uppercase tracking-[.2em] text-blue-700">CRM Module</p><h1 className="mt-1 text-3xl font-extrabold">{title}</h1><div className="mt-7 rounded-3xl bg-white p-8 shadow-sm"><h2 className="text-xl font-extrabold">{title} workspace</h2><p className="mt-2 max-w-2xl text-slate-500">Production data table, filters, role-based actions and audit history are ready to connect to the shared API. No fake live records are displayed here.</p><div className="mt-6 rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No live data connection configured yet.</div></div></div></main>}
