"use client";
import Link from "next/link"; import {CarFront,CalendarDays,CheckCircle2,ShieldCheck,KeyRound,ChevronDown} from "lucide-react"; import {useState} from "react"; import PageShell from "../PageShell";
const steps=[[CarFront,'Choose Your Vehicle','Select a car or bike that fits your trip.'],[CalendarDays,'Select Dates','Pick your pickup and return dates and times.'],[CheckCircle2,'Submit KYC','Share the required identity and driving documents.'],[ShieldCheck,'Make Payment','Confirm securely after reviewing your booking.'],[KeyRound,'Start Your Ride','Complete pickup, verify the vehicle and drive away.']];
const faqs=[['What documents are required?','A valid driving licence and the requested identity/KYC documents are required before handover.'],['How does pickup work?','After confirmation, Allino shares the pickup instructions. Verify the vehicle condition before starting the trip.'],['What happens at return?','Return the vehicle at the agreed time and location. Final checks are completed before the booking is closed.'],['Is my payment secure?','Payments are handled through the configured payment flow. For the current frontend, payment can be connected to a live gateway later.']];
export default function How(){const [open,setOpen]=useState(0);return <PageShell title="Renting with Allino is simple and hassle-free." kicker="HOW IT WORKS"><section className="section"><div className="container"><div className="steps">{steps.map(([Icon,title,text],i)=><div className="step" key={title as string}><span>{`0${i+1}`}</span><div className="step-icon"><Icon/></div><h3>{title as string}</h3><p>{text as string}</p>{i<4&&<div className="connector"><i/></div>}</div>)}</div></div></section><section className="section soft"><div className="container two-col"><div><div className="section-kicker">BEFORE YOU RIDE</div><h2>KYC, payment and pickup — made clear.</h2><p>We keep the process straightforward so you know what is required before your vehicle is handed over.</p><div className="checks single"><span>✓ Valid driving licence and KYC</span><span>✓ Vehicle condition check at pickup</span><span>✓ Clear rental and return instructions</span><span>✓ Customer support during your rental</span></div><Link className="btn primary" href="/book/">Book Your Ride →</Link></div><div className="faq"><div className="section-kicker">FAQ</div>{faqs.map(([q,a],i)=><div className="faq-item" key={q}><button onClick={()=>setOpen(open===i?-1:i)}><span>{q}</span><ChevronDown className={open===i?"rotate":""}/></button>{open===i&&<p>{a}</p>}</div>)}</div></div></section></PageShell>}
